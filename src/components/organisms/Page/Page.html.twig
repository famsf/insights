<section class="page {{ classes }}" id="{{ id }}">

    {% if pageType == 'coverPage' %}
      {% include "@molecules/CoverPage/CoverPage.html.twig"%}
    {% elseif pageType == 'twoCol' %}
      <div>
        Two Col
      </div>
    {% elseif pageType == 'slideShow' %}
    {% elseif pageType == 'annotatedImageObject' %}
    {% else %}
      <div class="page__content">
        <div class="content {{ contentClasses }}">
          {% if pageTitle %}
            <h2 class="page-title">{{ pageTitle }}</h2>
          {% endif %}
          {{ pageContent }}
        </div>
      </div>
    {% endif %}
</section>
