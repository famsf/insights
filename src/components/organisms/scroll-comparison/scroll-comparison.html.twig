{% extends "@layouts/two_up--primary/two_up--primary.html.twig" %}
{% set left = left|merge({offset: left.offset ?: 'medium-offset-2'}) %}
{% set right = right|merge({offset: right.offset ?: ''}) %}
{% set classes = classes|merge(['scroll-comparisson'])}
