{% import "@macros/inklude.html.twig" as inklu %}
<div class="one_up--primary fluid {{ gridframe|default('grid-frame') }} {{ classes }}">
  <div class="grid-x grid-margin-x {{ grid }}">
    <div class="cell {{ cols|default('medium-10') }} {{ offset|default('medium-offset-1') }}">
      {{ inklu.de(components) }}
    </div>
  </div>
</div>
