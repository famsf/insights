{% import "@macros/inklude.html.twig" as inklu %}
<div class="modal reveal {{ full_screen ? 'full-screen' }} {{ class }}" id="{{ id }}" data-reveal data-animation-in="fade-in" data-animation-out="fade-out">
  {{ inklu.de(components) }}
  {% include "@atoms/modal__close-button/modal__close-button.html.twig" %}
</div>
