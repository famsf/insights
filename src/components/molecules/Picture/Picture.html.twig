<picture class="picture pictyre--{{ style ? style : 'vertical' }}">
  {% for src in srcset %}
    <source srcset="{{ srcset.url }}" media="(min-width: 350px)">
  {% endfor %}
 <img src="{{ url }}" alt="{{ alt }}">
</picture>
