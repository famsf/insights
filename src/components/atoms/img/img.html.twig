<img
  class="{{ classes }}"
  alt="{{ alt }}"
  width="{{ width }}"
  height="{{ height }}"
  ci-responsive
  ci-src="{{ url }}"
  {% if type %}
    ci-type="{{ type }}"
  {% endif %}
  {% if sizes %}
    ci-size="{{ sizes }}"
  {% endif %}
>
