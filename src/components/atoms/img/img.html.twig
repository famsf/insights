{##
 # @TODO: The sizes are hard-coded here; would be nice to pull them from a
 # variable or some other canonical source that feeds both this use and that of
 # CoverPage.js.
 #}
<img
  class="{{ classes }}"
  alt="{{ alt }}"
  width="{{ width }}"
  height="{{ height }}"
  ci-responsive
  ci-src="{{ url }}"
  ci-type="crop"
  ci-size="{xs: '50x100', sm:'100x125', md: '150x150', lg:'200x175', xl:'300x200'}"
>
