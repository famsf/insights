<!DOCTYPE html>
<html lang="en">
  <head>
      <meta charset="utf-8">
      <meta http-equiv="X-UA-Compatible" content="IE=edge">
      <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
      <link rel="shortcut icon" href="images/favicon.ico" type="image/vnd.microsoft.icon">
      <link rel="apple-touch-icon" href="images/touch-icon.png">
      <link rel="icon" href="images/favicon.png">
      <!--[if IE]><link rel="shortcut icon" href="images/favicon.ico"><![endif]-->
      <!-- or, set /favicon.ico for IE10 win -->
      <meta name="msapplication-TileColor" content="#f00">
      <meta name="msapplication-TileImage" content="/images/tileicon.png">

      <!-- Google Tag Manager -->
      <script>(function(w,d,s,l,i){w[l]=w[l]||[];w[l].push({'gtm.start':
      new Date().getTime(),event:'gtm.js'});var f=d.getElementsByTagName(s)[0],
      j=d.createElement(s),dl=l!='dataLayer'?'&l='+l:'';j.async=true;j.src=
      'https://www.googletagmanager.com/gtm.js?id='+i+dl;f.parentNode.insertBefore(j,f);
      })(window,document,'script','dataLayer','GTM-TPPZRZB');</script>
      <!-- End Google Tag Manager -->

      <title>
        {{ config.title }}
      </title>

      <meta name="description" content="{{ config.description }}">
      {% include "@atoms/og_tag/og_tag.html.twig" with {property: "title", content: config.title} only %}
      {% include "@atoms/og_tag/og_tag.html.twig" with {property: "description", content: config.description} only %}
      {% include "@atoms/og_tag/og_tag.html.twig" with {property: "type", content: "website"} only %}
      {% include "@atoms/og_tag/og_tag.html.twig" with {property: "url", content: "https://insights.famsf.org/la-prose/"} only %}
      {% include "@atoms/og_tag/og_tag.html.twig" with {property: "image", content: "https://live-famsf-insights.pantheonsite.io/sites/default/files/laprose/0_la-prose-title-card.jpg"} only %}

      {{ kstatic.scripts }}
      {% for script in config.scripts.header.kalastatic %}
        <script src="{{ script }}?{{ 'now'|date('U') }}"></script>
      {% endfor %}
      {% for script in config.scripts.header.all %}
        <script src="{{ script }}?{{ 'now'|date('U') }}"></script>
      {% endfor %}
      {{ kstatic.styles }}
      {% for sheet in config.stylesheets %}
        <link rel="stylesheet" type="text/css" href="{{ sheet }}?{{ 'now'|date('U') }}">
      {% endfor %}
  </head>
  <body class="{{bodyClass|clean_id}} {{classes}} {{bodyId|clean_id}}">
    <!-- Google Tag Manager (noscript) -->
    <noscript><iframe src="https://www.googletagmanager.com/ns.html?id=GTM-TPPZRZB"
    height="0" width="0" style="display:none;visibility:hidden"></iframe></noscript>
    <!-- End Google Tag Manager (noscript) -->
    <div id="main">
      {{ contents }}
    </div>
    {% for script in config.scripts.footer.kalastatic %}
      <script src="{{ script }}"></script>
    {% endfor %}
    {% if bodyId == 'hub' %}
      {% for script in config.scripts.footer.hub %}
        <script src="{{ script }}"></script>
      {% endfor %}
    {% else %}
      {% for script in config.scripts.footer.all %}
        <script src="{{ script }}"></script>
      {% endfor %}
    {% endif %}
  </body>
</html>
