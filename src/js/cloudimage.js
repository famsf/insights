(function (win) {
  win.document.addEventListener('DOMContentLoaded', function () {
    jScaler.process({
      TOKEN: 'a2coygopn',
      BASE_URL: 'https://insights.famsf.org'
    });
  });
}(window));
